schema_version = "1.3"
artifacts = ["/logs/artifacts/model.patch"]
[task]
name = "benzhi/transmute-resumable-import-manifests"
description = ""
authors = []
keywords = []
[metadata]
ext_id = "kh7baa7467be155c993b8b2d8e41baec"
task_id = "transmute-resumable-import-manifests"
display_title = "Add resumable import manifests with confirmation"
display_description = "Let users stage a batch of files into a recoverable import manifest, resume after interruptions, and see the final result of every item without mixing accounts."
original_title = "资料导入清单的恢复与确认"
category = "feature_request"
language = "python"
repository_url = "https://github.com/transmute-app/transmute"
base_commit_hash = "a9ff811498b5b71667ad199766a0e746ab37d508"
[verifier]
network_mode = "no-network"
environment_mode = "separate"
timeout_sec = 1800.0

[verifier.env]
[verifier.environment]
build_timeout_sec = 1800.0
cpus = 2
memory_mb = 8192
storage_mb = 20480

[[verifier.collect]]
command = "cd /app && mkdir -p /logs/artifacts && git config --global --add safe.directory /app && git diff --binary a9ff811498b5b71667ad199766a0e746ab37d508 HEAD > /logs/artifacts/model.patch"
timeout_sec = 300.0

[agent]
network_mode = "no-network"
timeout_sec = 10800.0
[environment]
build_timeout_sec = 1800.0
# docker_image is omitted in step 1: Harbor/Pier builds environment/Dockerfile.
# After the image is built and pushed, set e.g.
# docker_image = "<registry>/benzhi-swe:kh7baa7467be155c993b8b2d8e41baec-v1"
os = "linux"
cpus = 2
memory_mb = 8192
storage_mb = 20480
gpus = 0
mcp_servers = []

[environment.env]
[solution.env]
